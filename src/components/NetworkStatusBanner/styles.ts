import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  banner: {
    color: '#FFFFFF',
    textAlign: 'center',
    paddingVertical: 6,
    fontSize: 13,
    fontWeight: '600',
  },
});
